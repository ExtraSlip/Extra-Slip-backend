module.exports = ({ env }) => ({
    ckeditor: {
      enabled: true,
      config: {
        // Additional CKEditor configuration options
        plugin: {
          // Plugin configurations (optional)
        },
        editor: {
          // Editor configurations
        }
      }
    }
  });
  